subprojects {
    apply plugin: "groovy"

    dependencies {
        implementation localGroovy()
        implementation gradleApi()
    }

    rootProject.dependencies {
        runtime project(path)
    }
}
gradle